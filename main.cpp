#include <iostream>
typedef const long double constParameter;
typedef long double parameter;

//change these to edit the output of the script
constParameter bottomLength = 45.0; //aka c2c distance
constParameter backHubWheelRadius = 38.2;
constParameter topHubWheelRadius = backHubWheelRadius;
constParameter length = 45.0;

//function headers
parameter calculateArc(parameter degrees, parameter diameter);

int main() {
    parameter totalLength = bottomLength + (2 * sideLength);//calculates total length of non-degree related stuff
}

parameter calculateArc(parameter degrees, parameter diameter){
    return 0;
}

